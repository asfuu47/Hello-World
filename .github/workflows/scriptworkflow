name: scriptworkflow
on: push
jobs:
  build:
    name: job1
    runs-on: ubuntu-latest
    steps:
      - name: step one
        run: |
            echo "running myscript.sh"
            ./myscript.sh
